'use strict';

module.exports = {
    connection: {
        host: "localhost",
        port: 27017,
        database: "bookstore"
    },
    application: {
        port: 3000,
        DEBUG: true
    }
};